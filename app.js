const screenOperationSpan = document.querySelector('.number.operation');
const screenResultSpan = document.querySelector('.number.result');

const calculatorButtonNodeList = document.querySelectorAll('button');
calculatorButtonNodeList.forEach(button => button.addEventListener('click', e => {
    
}));
